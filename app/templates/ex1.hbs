{{page-title "Ex1"}}
<div class="card">
  <div class="card-header bg-primary text-light"> Ex1 : Note application</div>
  <div class="card-body">
  <Textarea class="form-control mb-3" @value={{this.content}}
  @input={{this.update}}/>
    <button class="btn btn-primary" {{on "click" this.save}}>Enregister</button>
    <button class="btn btn-primary" {{on "click" this.clear}}>Effacer</button>
  </div>
  <div class="alert {{this.style }}"> Il reste {{this.size}} caractères disponibles.</div>
  <div class="alert"> {{this.info}}</div>
</div>
{{outlet}}

