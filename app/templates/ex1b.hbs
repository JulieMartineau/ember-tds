{{page-title "Ex1b"}}
<div class="container">

  <div class="card">
    <div class="card-header bg-primary text-light">Traces des notes de l'application</div>
    <div class="card-body">

      <ul>
        {{#each @model as |mot|}}
          <li>{{mot}}</li>
        {{/each}}
      </ul>
      <LinkTo @route="ex1"><button class="btn btn-primary" >Retour</button></LinkTo>
    </div>
  </div>

</div>
{{outlet}}